X = func([3,2,3])
X = func(1,4,'single')
X = func(size(A))%根据现有数组克隆数据类型
X = func(sizep,'like',p)；%根据现有数组克隆大小和数据类型

%指定区间内的随机数
r = a + (b-a).* rand(N,1)%生成区间（a,b）内的N个随机数。
r = -5 + (5+5)*rand(10,1)

%随机整数
r = randi([10,50],1,5)%10和50间均匀分布的5个随机整数

%随机复数
a = rand +1i*rand

%随机数生成器
s = rng;
r = rand(1,5)


rng(s);
r1 = rand(1,5) %r1和r的值相同

%diag
diag(diag(m))%可生成除对角线之外全为0的数组
